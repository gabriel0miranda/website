server {
    listen 8080;
    location / {
      default_type text/html;
      content_by_lua '
        require("lapis").serve("app")
      ';
    }

    location /static/ {
      alias ../site/static/;
    }

    location /favicon.ico {
      alias ../site/static/favicon.ico;
    }
}
